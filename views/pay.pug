html
  body
    form(action="/charge", method="post")
      article
        label="Montant : " + my_amount + " EUR"
      script(
        src="//checkout.stripe.com/v2/checkout.js",
        class="stripe-button",
        data-key=keyPublishable,
        data-locale="auto",
        data-description="Formulaire de paiement",
        data-amount=my_amount*100)
